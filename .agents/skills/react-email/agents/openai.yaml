interface:
  display_name: React Email
  short_description: Author React Email templates when the dependency is installed.
  default_prompt: Use $react-email for the current task.
policy:
  allow_implicit_invocation: true
