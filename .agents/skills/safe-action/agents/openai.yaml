interface:
  display_name: Safe Action
  short_description: "Build typed next-safe-action server workflows"
  default_prompt: Use $safe-action for the current task.
policy:
  allow_implicit_invocation: true
