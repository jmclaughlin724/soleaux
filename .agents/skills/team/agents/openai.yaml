interface:
  display_name: "Team"
  short_description: "Design and run parallel agent workflows"
  default_prompt: "Use $team to split this task into bounded parallel workstreams, coordinate the agents, and reconcile their results."
policy:
  allow_implicit_invocation: true
