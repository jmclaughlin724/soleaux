interface:
  display_name: React
  short_description: Build, optimize, compose, animate, or review React interfaces.
  default_prompt: Use $react for the current React interface task.
policy:
  allow_implicit_invocation: true
