interface:
  display_name: "Debug"
  short_description: "Evidence-led diagnosis and narrow verified fixes"
  default_prompt: "Use $debug to reproduce this failure, identify its root cause, and verify the narrowest authorized fix."
policy:
  allow_implicit_invocation: true
