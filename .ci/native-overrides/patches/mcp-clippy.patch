diff --git a/daemon/mcp/src/lib.rs b/daemon/mcp/src/lib.rs
index 2e6b079..2a6a62f 100644
--- a/daemon/mcp/src/lib.rs
+++ b/daemon/mcp/src/lib.rs
@@ -440,15 +440,8 @@ impl PublicMcpServer {
     }
 
     pub fn handle_json_rpc(&self, request: &Value) -> Option<Value> {
-        let id = request.get("id").cloned();
+        let id = request.get("id").cloned()?;
         let method = request.get("method").and_then(Value::as_str).unwrap_or_default();
-        if id.is_none() {
-            return match method {
-                "notifications/initialized" | "notifications/cancelled" => None,
-                _ => None,
-            };
-        }
-        let id = id.unwrap_or(Value::Null);
         if !is_supported_rpc_method(method) {
             return Some(json_rpc_error(
                 id,