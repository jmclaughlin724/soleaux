# Phase 3 Results

```text
Status: DEFERRED — never run
Phase 3: DEFERRED (optional claims-gate)
productionClaimAllowed: false
```

Deferred by owner direction on 2026-08-03; the experiment gates efficacy
claims only and blocks nothing. No live model results exist.

This file may be populated only after:

- `STATUS.json` is frozen;
- baseline and treatment runs complete;
- every run validates;
- scoring and independent verification complete.

Required report sections:

1. experiment manifest;
2. model/client/parameters;
3. repositories and commits;
4. task-level raw results;
5. aggregate correctness;
6. context/tool measurements;
7. fail-closed incidents;
8. limitations;
9. independent-verification receipt;
10. conclusion without changing `productionClaimAllowed`.
