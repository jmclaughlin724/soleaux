name: Apply P4-019 transactional edit rollback

on:
  push:
    branches:
      - fix/editor-transactional-rollback-0.4.0-dev.5
    paths:
      - .ci/apply-p4-019.py
      - .github/workflows/apply-p4-019.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: apply-p4-019-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apply:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/editor-transactional-rollback-0.4.0-dev.5
          fetch-depth: 0

      - name: Install native build prerequisites
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            clang cmake libclang-dev llvm-dev pkg-config protobuf-compiler

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.97.1
          components: rustfmt, clippy

      - name: Apply P4-019
        run: python3 .ci/apply-p4-019.py

      - name: Focused native gates
        working-directory: native
        shell: bash
        run: |
          set -euo pipefail
          cargo fmt --all
          cargo fmt --all --check
          cargo check -p soleaux-mcp --all-targets --all-features --locked
          cargo clippy -p soleaux-mcp --all-targets --all-features --locked -- -D warnings
          cargo test -p soleaux-mcp --all-features --locked \
            post_write_failure_restores_source_preview_and_receipt
          cargo test -p soleaux-mcp --all-features --locked \
            preview_is_no_write_and_apply_revalidates_hash

      - name: Commit normal source and remove one-shot files
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .ci/apply-p4-019.py
          git rm .github/workflows/apply-p4-019.yml
          git add native/daemon/mcp/src/editor.rs
          git diff --cached --check
          git commit -m "fix(editor): roll back failed post-write bookkeeping"
          git push origin HEAD:fix/editor-transactional-rollback-0.4.0-dev.5
