name: Verify P4-019 exact normal source

on:
  pull_request:
    branches:
      - main
    paths:
      - native/**
      - .github/workflows/verify-p4-019-final.yml
      - P4-019-SOURCE-VALIDATION.md
  push:
    branches:
      - fix/editor-transactional-rollback-0.4.0-dev.5
    paths:
      - native/**
      - .github/workflows/verify-p4-019-final.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: verify-p4-019-${{ github.event.pull_request.head.sha || github.sha }}
  cancel-in-progress: true

jobs:
  native:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    env:
      SOURCE_COMMIT: ${{ github.event.pull_request.head.sha || github.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.SOURCE_COMMIT }}
          fetch-depth: 0

      - name: Install native build prerequisites
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            clang cmake libclang-dev llvm-dev pkg-config protobuf-compiler python3-jsonschema

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.97.1
          components: rustfmt, clippy

      - name: Verify exact source and locked identity
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$SOURCE_COMMIT"
          test ! -e .ci/apply-p4-019.py
          test ! -e .github/workflows/apply-p4-019.yml
          grep -R 'rollback_after_failure' native/daemon/mcp/src/editor.rs
          grep -R 'post_write_failure_restores_source_preview_and_receipt' \
            native/daemon/mcp/src/editor.rs
          echo '89a2b783c4bd9c0ae834a5894dceb2c4abcaa8050dd0f57ed967a9c57e3a60fc  native/contracts/unified-mcp-profile-v2.json' | sha256sum -c -
          echo '3bbb53e84b0624f2a1de26bad7f2031b6a7cf0f7e892262d584347bd54b6003f  native/contracts/context-packet-v2.schema.json' | sha256sum -c -

      - name: Complete native and compatibility gate
        working-directory: native
        shell: bash
        run: |
          set -euo pipefail
          cargo fmt --all --check
          cargo check --workspace --all-targets --all-features --locked
          cargo clippy --workspace --all-targets --all-features --locked -- -D warnings
          cargo test --workspace --all-features --locked
          cargo build --release --workspace --all-features --locked
          ./target/release/soleaux --help >/dev/null
          ./target/release/soleaux --version
          ./target/release/soleauxd --help >/dev/null
          ./target/release/soleauxd --version
          python3 scripts/phase1_mcp_smoke.py \
            target/release/soleaux . /tmp/phase1-smoke.json
          python3 scripts/phase2_capability_smoke.py \
            target/release/soleaux target/release/soleauxd . /tmp/phase2-smoke.json
          if ! command -v cargo-audit >/dev/null 2>&1; then
            cargo install cargo-audit --locked
          fi
          cargo audit --deny warnings

      - name: Validate repository documentation
        run: python3 scripts/check_documentation_consistency.py

      - name: Write exact-head receipt
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p evidence
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          receipt = {
              "schemaVersion": "soleaux.phase4-task-receipt/v1",
              "task": "P4-019",
              "sourceCommit": os.environ["SOURCE_COMMIT"],
              "workflowRunId": os.environ["GITHUB_RUN_ID"],
              "conclusion": "success",
              "version": "0.4.0-dev.5",
              "productionClaimAllowed": False,
              "publicToolCeiling": 12,
              "gates": [
                  "post-write-rollback-regression",
                  "fmt",
                  "check",
                  "clippy",
                  "test",
                  "release-build",
                  "canonical-mcp-smoke",
                  "substitution-smoke",
                  "context-v2-smoke",
                  "audit",
                  "documentation",
              ],
          }
          Path("evidence/P4-019-NATIVE-GATE-RECEIPT.json").write_text(
              json.dumps(receipt, indent=2) + "\n",
              encoding="utf-8",
          )
          PY
          sha256sum evidence/P4-019-NATIVE-GATE-RECEIPT.json > evidence/SHA256SUMS

      - uses: actions/upload-artifact@v4
        with:
          name: soleaux-p4-019-${{ env.SOURCE_COMMIT }}
          path: evidence/
          if-no-files-found: error
          retention-days: 30
